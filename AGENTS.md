# 项目协作约定

## 默认规则

- 任何 Python 代码的新增、修改、重构，默认使用 `$python-strict-dev` Skill。
- 除非用户明确要求其他方式，否则按 `python-strict-dev` 中的严格编码规范执行。
- 项目文档、计划、说明性文字、注释性文案默认使用中文。
- 如果项目中的某个文件或外部接口已有稳定英文约定，允许在该局部范围内沿用英文。

## 执行说明

- 这些规则是当前仓库的默认约束，后续进入本仓库工作时应优先遵守。
- 如果用户在当前会话中给出更高优先级的明确指令，以用户指令为准。
